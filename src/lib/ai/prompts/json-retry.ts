/** Appended to user turn on first parse/Zod failure (single retry). */
export const GEMINI_JSON_RETRY_SUFFIX =
  "\n\nTrả lời đúng JSON theo schema đã nêu. Không markdown, không bọc trong code fence.";
